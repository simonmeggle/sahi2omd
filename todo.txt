initialize: w/c wird später durch warn/crit dargestellt, unfug

globale vars von lokalen trennen!


guid muss überall gleich heißen _> runuid (auch in DB!)

Wenn nagios.inc im DB mode eine nsca-Datei zurücklässt, ist dies eine Fehlermeldung, die passiv an Naigos gehen soll. 

nur das error-objekt an die Datenbank geben. Das "was ok" braucht die DB Nicht.  

Step names in Anführungszeichen

sahi2omd.log : 
17:38:13: Script ran in  seconds.